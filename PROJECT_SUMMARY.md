# 安居家电售后智能客服 + 上门报修预约 Agent — 项目总结

> 一份通俗、完整的项目阅读笔记，面向汇报与整体理解，不含代码级细节（代码细节见 `SPEC.md`）。

---

## 1. 项目是什么

**安居家电售后智能客服**是一个面向家电品牌的**智能售后客服 + 上门维修报修调度系统**。

它做的不是一张普通的报修表单，而是把售后客服中心的高频工作自动化：理解客户想**报修**、**咨询**还是**投诉**，识别报修的家电品类与故障，追问补齐地址与时间，匹配合适的维修工程师，检查上门档期，生成报修工单并跟踪进度；同时按客户手机号打通购买订单，随时回答"还在保修期吗""工单什么进度了"，并在客户不满时登记转人工——像一个"AI 售后前台"。

技术选型：**FastAPI + LangChain + FAISS + SQLite**，大模型与 Embedding 兼容 OpenAI 格式（Qwen / DeepSeek / 智谱 / OpenAI / Azure 可切换）。

---

## 2. 为什么做这个项目（背景与动机）

家电售后场景中，客服坐席需要同时处理大量复杂事务：

- 报修登记：品类、故障、地址、联系方式的逐项确认与补录
- 上门派单：在多名工程师中按技能、区域、档期做匹配与冲突规避
- 保修口径：客户订单的保修状态与收费预期
- 工单进度查询与投诉安抚、转人工

传统人工坐席的痛点：**沟通成本高、字段遗漏导致返工、派单冲突、响应不稳定**。

项目尝试用 AI Agent 重构这一流程：让系统像一名熟练客服那样主动理解需求、补齐关键信息，再分发给对应的专业 Agent 处理。系统是家电售后的原型，也可**泛化到任何需要工单调度、上门服务与智能客服的行业**（如 IT 运维上门、家居安装、家电清洗等），同时是 AI Agent 工程化、RAG、结构化抽取与状态机编排的综合实践项目。

---

## 3. 核心能力一览

| 能力 | 一句话说明 |
| --- | --- |
| 智能任务分类 | 先判断客户意图（报修/咨询/投诉/无关），再把请求路由给对应 Agent |
| 多 Agent 协作 | 客服调度、报修专员、售后顾问、用户行为四个 Agent 分工，避免单个模块职责膨胀 |
| 报修工单闭环 | 报修登记 → 自动匹配工程师 → 生成工单 AX… → 全程可查进度、后台可派单流转 |
| 保修查询 | 按手机号查名下订单，动态计算在保/超保与截止日，纯查库零 LLM 幻觉 |
| 投诉转人工 | 单轮登记诉求 + 30 分钟回电承诺，落库转人工记录 |
| RAG 知识咨询 | FAISS 向量检索知识库 + 大模型生成回答，支持流式输出 |
| 用户行为分析 | 记录报修行为，统计常用品类/故障/时段/工程师，支撑回访 |
| 会话隔离 + 分层记忆 | 每会话独立运行时（刷新/重启可续谈、多会话互不串场）；老客户历史要点按语义/时效/重要度召回注入 |
| AutoDream 自动沉淀 | 老客户（≥5 次会话且跨度 ≥24h）行为离线回放：偏好置信度累计 + 冲突降权 + 画像记忆去重轮换，任务锁/断点幂等 |
| 主管 ReAct 工具化 | 子 Agent/确定性流程登记为主管工具（注册表含风险/模型分级元数据）；分类即工具选择、工具清单注入提示词、子任务无关触发重新规划（带轮次上限）、规划复盘可审计 |
| 模型分级（双通道） | `main` 大模型承接生成，`fast` 小/轻量模型通道承接任务分类、相关性判定、槽位抽取等高频结构化调用；未配置 fast 自动回退 main，零行为差异 |
| 后台管理 | 工单管理、工程师排班、知识库管理、售后回访页齐备 |
| 兜底降级 | 缺信息逐项追问、无 Key 环境模板化兜底，客户永远拿到可用结果 |

---

## 4. 系统架构（一句话版）

项目采用**五层架构**，核心原则是**下层不能反向调用上层**：

```text
Web 层（页面与入口）      web/ 、app.py
   ↓
API 层（外部接口）        api/
   ↓
Agents 层（AI 智能）      agents/   ← 项目核心
   ↓
Services 层（业务逻辑）   services/
   ↓
DB 层（数据访问）         db/
```

- Web 层调用 API 层，API 层调用 Agents / Services，Agents 调用 Services，Services 调用 DB。
- 禁止反向调用，禁止跨层跳级。这保证了业务逻辑、数据访问与接口编排的清晰边界，避免循环依赖。

**一句话理解**：Agent 只关心"怎么想"（智能流程与话术），Service 只关心"怎么算"（工单/保修/派单规则），Repository 只关心"怎么存"（数据访问）。

---

## 5. 四个 Agent 的分工

### 5.1 任务分类 Agent —— 客服调度（主调度器）

所有聊天请求的第一站。流程是：**意图分析（观察）→ 工具选择（规划）→ 子 Agent 执行 → 信号再规划**——把客服调度做成一个轻量 ReAct 循环：

- **工具化**：子 Agent 与确定性流程登记为主管工具（`agents/supervisor/tool_registry.py` 四件套：报修登记/售后咨询/转人工/兜底，每件标注风险分级与模型分级元数据）；LLM 分类返回类别即选中工具，工具清单注入分类提示词让两者语义对齐；每轮回合写入规划复盘（category→tool_id，可审计）；
- 用 LLM（fast 小模型通道）判断客户输入属于哪类任务：`appointment`（报修/预约上门）/ `query`（售后咨询与订单/保修/工单进度查询）/ `complaint`（投诉/转人工）/ `other`（无关）；
- 维护**对话状态机**：空闲态（CLASSIFY）→ 报修态（APPOINTMENT）或咨询态（CONSULT）→ 完成后回到空闲态；
- 投诉类直接在调度层落库转人工记录并回执，不进入子流程；
- 子 Agent 判断为无关的请求会**转交回调度重新规划**（观察→再选工具，设有轮次上限，防递归死循环）；
- 不在能力范围内的问题给出"抱歉 + 能力清单"式友好回复（兜底工具）。

### 5.2 报修专员 Agent —— 核心业务流程

流程：**结构化抽取 → 缺失追问 → 工程师匹配 → 建单派单**。

1. **信息抽取**：大模型把客户口语变成结构化 JSON（品类、故障、地址、手机号、上门时间、指定工程师等）。"空调不制冷，手机 13800138000，想报修上门"会被抽成 `品类=空调，故障=不制冷，手机=138…`；多轮对话逐步补全。
2. **缺失追问**：缺地址问地址、缺时间给 9:00-18:00 槽位提示，直到五项必需信息齐全。
3. **工程师匹配**：
   - 客户指定了工程师 → 先查档期；没空则**按技能向量相似度找专长最接近的替代工程师**，生成推荐话术请客户确认（"是/好"就换人，"不/不要"则尊重选择）；
   - 客户没指定 → 按 区域软偏好（地址命中的城区优先）→ 技能相似度排序 → 逐个检查可用时段 的顺序匹配，区域内没空自动扩大到全量工程师。
4. **建单派单**：生成工单号 `AX+YYYYMMDD+序号`，写工程师忙档，记录客户行为（按手机号），并按订单**动态给出在保/超保提示**（在保免上门费维修费、超保先报价）。

### 5.3 售后顾问 Agent —— RAG 知识问答

流程：**查询短路 → 咨询判定 → 知识检索 → 流式回答**。

- **查询短路**：订单保修、工单进度类意图（手机号 + 保修关键词、单号 + 进度关键词）由纯函数正则直接查库，模板化返回，不消耗 LLM 也不进 RAG；
- 其他问题先判定是否属于售后咨询（政策、收费、故障排查等）；不是就交回主调度器；
- 是则走 RAG：问题向量化 → FAISS 检索 top-3 知识文档 → 连同知识拼进提示词 → 大模型流式回答；
- 默认知识库含 12 条家电售后条目（保修政策/收费标准/分品类故障排查/退换货/服务时间等），可经管理页增删改查。

### 5.4 用户行为 Agent —— 后台偏好学习

流程：**行为记录 → 模式分析 → 偏好更新 → 回访提醒**。

- 每次报修与咨询留下行为记录（客户手机号、品类、故障、时段、工程师、会话）；
- 后台统计出客户的常用工程师、常用品类与故障、常用上门时段；
- 当"累计报修 ≥ 3 次 或 距上次报修 ≥ 30 天"时判定应回访；
- 生成回访时会先查常用工程师今天/明天 9:00-18:00 的空闲时段（最多 3 个），再由大模型生成保养建议 / 保修到期 / 满意度回访话术。

---

## 6. 一次完整对话长什么样

**场景：客户报修（缺信息 → 补齐 → 成功）**

```
客户：空调不制冷，手机 13800138000，想报修上门
客服调度：[THOUGHT] 检测到家电报修任务，转给报修专员
报修专员：请问上门维修的地址是哪里？……请问希望工程师几点上门？服务时间 9:00-18:00……
客户：地址海淀区中关村某小区3号楼，明天下午3点上门
报修专员：[THOUGHT] 匹配工程师…找到张建国（海淀区）…
        您的报修已登记成功！- 报修单号：AX2026090305 - 工程师：张建国
        上门时间：2026-09-04 15:00-17:00
        查询到您名下该产品仍在保修期内（保修至2030-06-14），本次上门免收上门费和维修费……
```

**场景：客户查保修 / 工单进度**

```
客户：帮我看看 13800138000 名下的空调还在保修期吗
售后顾问：手机号 13800138000 名下共查询到 3 条订单：
        · 空调（…2024年06月15日购买）：在保修期内，保修至2030年06月14日…
        · 洗衣机（…2021年03月20日购买）：已超出保修期，可安排付费维修…
```

```
客户：报修单 AX2026090305 现在什么进度了
售后顾问：您的报修单 AX2026090305（空调，故障：不制冷）当前状态：已派单，工程师将联系您。
        服务工程师：张建国。预约上门时间：2026-09-04 15:00。
```

**场景：投诉 / 无关闲聊**

```
客户：你们的服务让我很不满意，我要投诉，手机号 13800138000
客服调度：您好，已为您登记转人工处理，售后专员将在30分钟内回电 13800138000……
客户：今天天气怎么样？
客服调度：抱歉，我暂时无法处理这类任务。安居家电售后客服可以协助您：家电报修登记、
        售后政策咨询、订单保修查询、投诉转人工。请问有什么可以帮您？
```

---

## 7. 关键技术点

| 技术点 | 做了什么 |
| --- | --- |
| LLM 结构化抽取 | 提示词让大模型输出**纯 JSON** 作为 Agent 与业务层之间的稳定契约；解析失败有全"未知"降级结构，流程不崩 |
| 状态机驱动对话 | CLASSIFY / APPOINTMENT / CONSULT 三态流转，多轮对话按状态续接，无需每轮重复分类 |
| Embedding + FAISS | 知识检索用内积相似度（IndexFlatIP）；工程师技能匹配用向量最近邻（IndexFlatL2），实现"指定工程师没空 → 推荐技能相近的替代工程师" |
| 纯函数查询短路 | 保修/工单进度等高确定性需求先于一切 LLM 调用走查库模板，防幻觉、防死循环、省成本 |
| RAG 可控回答 | 回答基于知识库文档而非自由发挥；检索不到时给明确兜底话术 |
| 流式输出 | 全链路 AsyncGenerator 逐 token 输出，前端按 `[THOUGHT]`（思考过程）/ `[REPLY]`（回复正文）/ `[SIGNAL]` / `[ERROR]` 令牌前缀渲染 |
| 确定性派单 | 档期冲突校验、工单状态机白名单、忙档写/释放全部纯逻辑，离线可测 |
| 模型工厂 | 一套代码切换 Qwen / DeepSeek / 智谱 / OpenAI / Azure，聊天与 Embedding 分开配置 |
| 懒加载解耦 | Services 层按需延迟导入，规避 Agent 与 Service 之间的循环依赖 |
| 全链路兜底 | 分类异常→"其他"；解析失败→追问；无 Embedding Key→跳过向量索引；生成失败→模板消息 |

---

## 8. 数据资产（12 张表）

| 表 | 记录什么 | 备注 |
| --- | --- | --- |
| engineers | 工程师档案（姓名/品类技能/服务区域） | 内置 8 名默认工程师 |
| engineer_schedules | 排班与忙档（busy/free 时间段） | 冲突判定 = busy 时段重叠，工单完成/取消即释放 |
| repair_tickets | 报修工单（单号/客户/品类/故障/地址/时段/状态/工程师） | 状态机 pending→assigned→in_progress→completed（前三态可取消） |
| orders | 客户购买订单（手机号/品类/型号/购买日期/保修年限） | 演示种子 5 条，保修期查询时动态计算 |
| human_handovers | 投诉/转人工记录（客户/诉求/关联工单） | 独立成表：投诉可能发生在无工单时 |
| knowledge_documents | 知识库文档（内容/分类/关键词/向量） | 默认 12 条家电售后知识，软删除，改动后重建索引 |
| user_behaviors | 客户行为日志（报修/咨询） | 客户以手机号标识 |
| user_preferences | 偏好（工程师/时段/品类/故障） | 置信度=出现次数累加 |
| user_recommendations | 推荐/回访记录 | 调度器与回访页产出 |
| chat_sessions | 会话快照（session_id/绑定客户/状态/槽位/窗口/摘要） | 每轮单行覆盖写，重启按行还原 |
| user_memories | 客户长期记忆（报修/咨询要点/客户画像 + 重要度 + 向量） | 软删除，按 0.6 语义+0.3 时效+0.1 重要度召回；画像每人至多一条活跃 |
| dream_checkpoints | AutoDream 回放断点 + 任务锁（每人一行） | 增量回放幂等；持锁超 30 分钟视为崩溃残留自动接管 |

---

## 9. 项目亮点（汇报可用）

1. **任务分类 + 状态机降低系统复杂度**：不做"万能 Agent"，先分类再分发给专业模块——新增能力只需注册分类枚举与路由器。
2. **完整业务闭环而非单点 Demo**：报修、派单、保修、进度、投诉、回访形成售后全链路，且后台可运营（工单管理页真实驱动状态）。
3. **RAG 让回答有据可依**：政策与知识由知识库维护，不写死在代码。
4. **确定性逻辑不依赖模型**：保修计算、档期冲突、状态机、单号生成全部纯函数——无 Key 也能演示报修登记全流程。
5. **多 Agent 协作的转交机制**：子 Agent 判无关 → 交回调度重分类，带递归上限防死循环，是真实工程里才会遇到的问题与解法。
6. **会话隔离 + 分层记忆**：每会话独立 Agent 运行时 + 快照写穿（刷新/重启不丢对话）；长期记忆按 `0.6 语义 + 0.3 时效 + 0.1 重要度` 召回 Top-5，客服能"记得老客户"——从工程上回答了大模型客服"上下文管理"的核心问题。
7. **AutoDream 离线沉淀引擎**：老客户行为自动回放成"偏好置信度 + 客户画像"——不打扰客户、无需人工建档案；同值置信度累加、漂移冲突自动降权（旧偏好淡出），画像语义去重轮换；任务锁 + 增量断点让调度在单机/多实例下都幂等可续。
8. **主管工具化 + 模型分级成本治理**：子 Agent 入口全部工具化（注册表元数据自带风险/模型分级，是权限白名单的源头）；分类、相关性判定、槽位抽取等高频结构化调用自动落到 fast 小模型通道，话术/RAG 长生成留在 main 大模型——".env 配一个模型名即启用，不配零差异回退"，省 token 成本与响应延迟，回答质量不掉。

---

## 10. 当前不足（坦诚清单）

- **无登录体系**：会话按浏览器 session_id 隔离、老客户以手机号识别，但无账号/权限体系；工单/订单按手机号归属客户；
- **转人工为登记制**：回执承诺 30 分钟回电，无真实外呼系统对接；
- **超保报价不闭环**：仅话术提示收费，无报价确认与支付链路；
- **无 Key 环境 RAG 降级**：跳过向量索引后知识问答退化为模板答复（报修/查询等核心链路不受影响）。

---

## 11. 使用与体验

```bash
# 1. 安装依赖并配置 .env（模型/Embedding 提供商 + Key）
pip install -r requirements.txt
cp .env.example .env

# 2. 启动（无 Key 也可启动演示核心链路）
python -m uvicorn app:app --host 127.0.0.1 --port 8001
```

启动后：

- 智能客服聊天：http://127.0.0.1:8001
- API 文档：http://127.0.0.1:8001/docs
- 报修工单管理（/tickets）、工程师（/engineers）、今日排班（/engineer_schedules）、知识库（/knowledge）、售后回访（/follow_ups）

系统启动自动完成五件事：读库建表并写入默认知识（构建向量索引）、写入 8 名默认工程师、写入演示订单（王芳 3 单可演示在保/超保）、启动回访提醒调度器、启动 AutoDream 离线沉淀调度器（每 60 分钟扫描一次老客户行为）。

---

## 12. 后续规划

1. **更强的 Agent 自主能力**：多轮推理、基于工单完成情况与满意度反馈优化话术与派单建议。
2. **更完整的协作**：行为 Agent 定时主动触达（消息推送位已留）、售后顾问感知工程师实时闲忙。
3. **生产化**：登录/权限、短信通知真实外呼、超保付费流程、Docker 部署与云数据库、标准日志监控。

---

*本文档用于项目整体理解与汇报；模块级技术细节与流程图请参阅同目录 `SPEC.md`。*
