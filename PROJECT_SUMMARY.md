# Smart Appointment AI Agent — 项目总结

> 一份通俗、完整的项目阅读笔记，面向汇报与整体理解，不含代码级细节（代码细节见 `SPEC.md`）。

---

## 1. 项目是什么

**Smart Appointment AI Agent** 是一个面向按摩门店场景的**智能预约与咨询系统**。

它做的不是一张普通的预约表单，而是把门店前台的高频工作自动化：理解用户是想**咨询**还是**预约**，判断服务偏好，匹配合适的技师，检查可用时间，生成预约结果，并在必要时结合天气、历史行为和偏好数据，给出更贴近用户的提醒与推荐——像一个"AI 智能前台"。

技术选型：**FastAPI + LangChain + FAISS + SQLite**，大模型与 Embedding 兼容 OpenAI 格式（Qwen / DeepSeek / 智谱 / OpenAI / Azure 可切换）。

---

## 2. 为什么做这个项目（背景与动机）

作者在一次按摩门店体验中观察到，前台人员需要同时处理大量复杂事务：

- 技师排班与预约调度
- 服务项目与价格咨询
- 临时改约与信息补录
- 记录顾客的技师偏好
- 收款确认等

随着技师与顾客数量增长，传统人工前台暴露出**沟通成本高、信息遗漏、排班冲突、服务体验不稳定**等问题。

项目因此尝试用 AI Agent 重构这一流程：让系统主动理解用户需求，再把不同任务分发给对应的专业 Agent 处理。它既是按摩门店的智能前台原型，也可以**泛化到任何需要排班、预约调度与智能客服的服务行业**（如诊所、美发、理疗、家政等），同时是一个学习 AI Agent 工程化、RAG 与业务自动化的综合实践项目。

---

## 3. 核心能力一览

| 能力 | 一句话说明 |
| --- | --- |
| 智能任务分类 | 先判断用户意图（预约/咨询/其他），再把请求路由给对应 Agent |
| 多 Agent 协作 | 任务分类、预约、咨询、用户行为四个 Agent 分工，避免单个模块职责膨胀 |
| RAG 知识咨询 | FAISS 向量检索知识库 + 大模型生成回答，支持流式输出 |
| 智能预约管理 | 按用户需求、技师专长、历史偏好、可用档期匹配技师，辅助完成预约 |
| 用户行为分析 | 记录交互与预约行为，统计偏好模式，驱动个性化推荐 |
| 个性化提醒 | 预约成功结合实时天气生成温馨提示；久未到店生成回访邀约 |
| 数据管理 | 知识库、技师、用户行为数据可增删改查，数据变化后自动维护向量索引 |
| 兜底降级 | 信息不足追问、模型异常模板回复，保证用户永远拿到可用结果 |

---

## 4. 系统架构（一句话版）

项目采用**五层架构**，核心原则是**下层不能反向调用上层**：

```text
Web 层（页面与入口）      web/ 、app.py
   ↓
API 层（外部接口）        api/
   ↓
Agents 层（AI 智能）      agents/   ← 项目核心
   ↓
Services 层（业务逻辑）   services/
   ↓
DB 层（数据访问）         db/
```

- Web 层调用 API 层，API 层调用 Agents / Services，Agents 调用 Services，Services 调用 DB。
- 禁止反向调用，禁止跨层跳级。这保证了业务逻辑、数据访问与接口编排的清晰边界，避免循环依赖。

**一句话理解**：Agent 只关心"怎么想"（智能流程），Service 只关心"怎么算"（业务规则），Repository 只关心"怎么存"（数据访问）。

---

## 5. 四个 Agent 的分工

### 5.1 任务分类 Agent —— 主调度器

所有聊天请求的第一站。流程是：**意图分析 → Agent 路由 → 响应协调**。

- 用 LLM 判断用户输入属于哪类任务（预约 / 查询 / 支付 / 统计 / 其他）；
- 维护**对话状态机**：空闲态（CLASSIFY）→ 预约态（APPOINTMENT）或咨询态（CONSULT）→ 完成后回到空闲态；
- 把任务分发给预约 Agent 或咨询 Agent；
- 子 Agent 处理不了的问题（无关请求）会**转交回分类器重新分类**，避免卡死在错误状态；
- 不在能力范围内的问题（如支付、统计，或纯闲聊）给出友好的"暂不支持"回复。

### 5.2 预约 Agent —— 核心业务流程

流程：**解析预约需求 → 技师匹配 → 预约确认 → 落库提醒**。

1. **信息抽取**：大模型把用户口语变成结构化 JSON（性别、时间、时长、项目、偏好、指定技师等）。"今天下午 3 点 1 小时按摩"会被抽成 `时间=今天15:00，时长=60分钟，项目=按摩`。每一轮只合并新增信息，多轮对话中逐步补全。
2. **缺失追问**：信息不齐就针对性追问（"请问您需要什么服务项目？""男技师还是女技师？"）。
3. **技师匹配**：
   - 用户指定了技师 → 先查档期；没空则**用向量相似度找专长最接近的其他技师**，生成推荐话术请用户确认（"是/好"就换人，"不/不要"则尊重选择）；
   - 用户没指定 → 按 性别筛选 → 偏好筛选（按专长向量匹配）→ 逐个检查可用时段 的顺序匹配，偏好技师没空会自动回退到全量技师。
4. **落库与提醒**：预约成功写入排班表（标记 busy），记录一条用户行为，再调用**天气工具**（OpenWeatherMap）生成结合天气的温馨提示。

### 5.3 咨询 Agent —— RAG 知识问答

流程：**咨询判定 → 知识检索 → 流式回答**。

- 先判定问题是否属于咨询类（服务、价格、营业时间、地址等）；不是就交回主调度器；
- 是则走 RAG：把问题向量化 → FAISS 在知识库中检索最相关的 top-3 文档 → 连同知识上下文拼进提示词 → 大模型生成自然语言回答（流式输出）；
- 知识库中的内容覆盖营业时间、服务项目与价格、技师情况、门店地址、各项目功效、预约政策、会员服务等 10 条默认知识，可通过管理页面增删改查。

### 5.4 用户行为 Agent —— 后台偏好学习

流程：**行为记录 → 模式分析 → 偏好更新 → 个性化推荐**。

- 每次预约与咨询都会留下行为记录（时间、项目、技师、会话）；
- 后台统计出用户的最爱技师、最常用服务与时长；
- 当"累计预约 ≥ 2 次 且 距上次预约 ≥ 30 天"时，判定该发回访提醒；
- 生成提醒时会先查最爱技师最近的空闲时段（今天或明天 9:00–22:00，最多 3 个），再由大模型生成一条温暖、提到技师与习惯的邀约消息。

---

## 6. 一次完整对话长什么样

**场景：用户想预约**

```
用户：帮我预约今天下午3点的按摩，1小时
归类机器人：[THOUGHT] 这是预约任务，转给预约机器人
预约机器人：请问您希望选男技师还是女技师？
用户：女技师，力气小的
预约机器人：[THOUGHT] 按性别+偏好筛选→查找空闲技师…
        已为您预约技师：李娜。预约成功！
        今天北京天气…，出行请注意防晒，期待与您相遇
```

**场景：用户想咨询**

```
用户：肩颈推拿有什么用？
归类机器人：[THOUGHT] 这是咨询任务，转给咨询机器人
咨询机器人：（检索知识库中"肩颈推拿"相关内容后回答）
        肩颈推拿专门针对颈椎和肩部问题…
```

**场景：预约中聊了别的（转交演示）**

```
用户（预约到一半）：今天天气怎么样？
预约机器人：[REPLY] 与预约无关，已交给归类机器人处理
归类机器人：（重新分类，回答天气问题后引导回预约）
```

---

## 7. 关键技术点

| 技术点 | 做了什么 |
| --- | --- |
| LLM 结构化抽取 | 用提示词让大模型输出**纯 JSON** 作为 Agent 与业务层之间的稳定契约；解析失败有全"未知"降级结构，流程不崩 |
| 状态机驱动对话 | CLASSIFY / APPOINTMENT / CONSULT 三态流转，多轮对话按状态续接，无需每轮重复分类 |
| Embedding + FAISS | 知识检索用内积相似度（IndexFlatIP）；技师专长匹配用向量最近邻（IndexFlatL2），实现"这个技师没空→推荐专长相近的技师" |
| RAG 可控回答 | 回答基于知识库文档而不是自由发挥；检索不到时提供明确的兜底话术（"请致电咨询"等） |
| 流式输出 | 全链路 AsyncGenerator 逐 token 输出，前端按 `[THOUGHT]`（思考过程）/ `[REPLY]`（回复正文）等令牌前缀渲染 |
| 工具调用 | 用 LangChain `create_openai_tools_agent` 封装天气工具，实现"预约成功 + 天气关怀"的复合回复 |
| 模型工厂 | 一套代码切换 Qwen / DeepSeek / 智谱 / OpenAI / Azure，聊天与 Embedding 分开配置 |
| 懒加载解耦 | Services 层按需延迟导入，规避 Agent 与 Service 之间的循环依赖 |
| 全链路兜底 | 分类异常→"其他"；解析失败→追问；天气失败→固定文案；生成失败→模板消息 |

---

## 8. 数据资产（6 张表）

| 表 | 记录什么 | 备注 |
| --- | --- | --- |
| engineers | 技师档案（姓名/性别/专长） | 内置 10 名默认技师 |
| engineer_schedules | 排班与预约占用（busy/free 时间段） | 冲突判定 = busy 时段重叠 |
| knowledge_documents | 知识库文档（内容/分类/关键词/向量） | 支持软删除，改动后自动重建索引 |
| user_behaviors | 用户行为日志（预约/咨询等） | 含时间、项目、技师、会话 ID |
| user_preferences | 用户偏好（技师/时段/服务/时长） | 置信度=出现次数累加 |
| user_recommendations | 推荐/回访记录 | 已发送标记 |

---

## 9. 项目亮点（面试/汇报可用）

1. **用任务分类降低系统复杂度**：不做一个"万能 Agent"，而是先分类再分发给专业模块——咨询、预约、行为分析相互独立，新增 Agent 只需注册分类器与路由器。
2. **用 RAG 让回答有据可依**：业务知识由知识库维护，回答基于可控知识源而非大模型自由发挥。
3. **用行为数据让推荐个性化**：偏好从历史行为中学习，推荐不是从零开始询问。
4. **分层架构保证可维护性**：每层职责单一，修改隔离。
5. **为真实业务预留扩展**：模型提供商可切换、MCP 外部服务位已留、后台调度框架已就绪、数据层预留迁移路径。

---

## 10. 当前不足（坦诚清单）

- **单用户模型**：所有行为挂在 default_user 下，全局单会话，未做用户体系与数据隔离；
- **推荐调度为占位**：定时框架（每天 9/14/19 点 + 每 2 小时）已运行，但具体推荐生成逻辑待实现；
- **部分分类未落地**：分类器能识别支付（pay）、统计（statistics）任务，但路由器暂不支持；
- **口径不一致**：营业时间在配置（12:00–22:00）与知识库/查档逻辑（9:00–22:00）间不统一；
- **兼容层残留**：部分老代码路径直接访问 DB，未完全收敛到 Services 层。

---

## 11. 使用与体验

```bash
# 1. 安装依赖并配置 .env（模型/Embedding 提供商 + Key）
pip install -r requirements.txt
cp .env.example .env

# 2. 启动
python -m uvicorn app:app --host 127.0.0.1 --port 8000 --reload
```

启动后：

- 聊天与预约：http://127.0.0.1:8000
- API 文档：http://127.0.0.1:8000/docs
- 知识库管理、技师管理、技师排班、用户行为分析均有独立页面

系统启动时会自动完成三件事：写入默认知识并构建向量索引、写入 10 名默认技师、启动推荐调度器。

---

## 12. 后续规划（项目作者已列）

1. **更强的 Agent 自主能力**：自我反思、多轮推理链、基于真实反馈优化推荐。
2. **更完整的协作**：Agent-to-Agent 直接通信、行为 Agent 定时主动触达、跨 Agent 上下文记忆打通。
3. **生产化**：登录/权限/数据隔离、异常边界覆盖、检索性能与缓存优化、Docker 部署与云数据库、标准日志监控。

---

*本文档用于项目整体理解与汇报；模块级技术细节与流程图请参阅同目录 `SPEC.md`，面试讲解请参阅 `INTERVIEW_SCRIPT.md`。*
